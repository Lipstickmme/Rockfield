# Supplied photography

Drop real artwork here, or into `../bank/`, named `rockfield1` … `rockfield6`
in any of `.webp`, `.avif`, `.jpg`, `.jpeg`, `.png` or `.svg`. The next build
picks it up in place of the generated placeholders and says so in the build
log. Which slot each name fills is set in `src/data/images.json`.
